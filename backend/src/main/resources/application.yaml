spring:
  jpa:
    open-in-view: false

  data:
    redis:
      repositories:
        enabled: false

  web:
    resources:
      add-mappings: false

  cloud:
    openfeign:
      client:
        config:
          open-api:
            url: https://open.api.nexon.com/maplestory/v1
          maple:
            url: https://maplestory.nexon.com

jasypt:
  encryptor:
    password: ${JASYPT_PASSWORD}
